To connect from Linux to Windows via SSH:

1. On Windows machine:
- Enable OpenSSH Server feature in Settings > Apps > Optional Features
- Set up SSH keys or password authentication

2. From Linux terminal:
- Use command: ssh username@windows-ip-address
- Run PowerShell commands after connecting

Make sure both machines are on same network and firewall allows SSH traffic.
